version: '3.3'

services:
  node:
    container_name: colour-challenge
    build: .
    volumes:
      - ./data:/srv/app/data
    labels:
      domain: colourchallenge.studiole.uk
    networks:
      - traefik
networks:
  traefik:
    external:
      name: traefik_web